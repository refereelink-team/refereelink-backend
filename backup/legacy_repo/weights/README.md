# Weights Directory

Store downloaded model weights for local examples here.

The soccer demo setup script writes `.pt` files into this directory so model
assets are managed separately from sample videos in `examples/soccer/data/`.
